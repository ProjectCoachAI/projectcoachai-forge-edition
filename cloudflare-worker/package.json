{
  "name": "projectcoachai-stripe-api-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for ProjectCoachAI Stripe API integration",
  "main": "stripe.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
